version: 1.0

# server
server_pid_path: .pid
server_pidfile: server.pid
server_ip: 127.0.0.1
server_port: 31066

# data
data_path: data

# log
log_path: log
log_file_name: main.log
log_format: "%(asctime)s(%(module)s:%(lineno)d)%(levelname)s:%(message)s"
log_in_date: false # if you set it true, log file will be saved in a date directory. (A date directory will be automatically generated.)
history_path: log/history

# callback run time
running_time: 1000 # in ms