# KoreanFA 개발 규칙

- 이 저장소는 PyPI 패키지를 만든다. 배포·패키징 방식은 PyPI 공식 문서와 신뢰할 수
  있는 공개 프로젝트의 일반적 패턴을 먼저 확인해 따른다. 임의의 배포 방식을 만들지 않는다.
- 배포를 제안하기 전, 아카이브·설치 용량, 의존성, 설치·사용 경험이 공개 패키지의
  상식적인 수준인지 확인한다.
- 문서에 공개한 Python API와 CLI는 모두 배포 계약이다. 배포 단계 전에는 각 경로를
  로컬에서 실제로 실행하고, 한국어·일본어 정렬도 검증한다.
- workflow는 git에 올리기 전에 로컬에서 점검·보완한다. GitHub Actions는 주된 디버깅
  환경이 아니라 로컬 검증 뒤의 독립적인 최종 확인용으로 사용한다.
- 사용자가 해당 작업을 명시적으로 허용하기 전에는 `git add`, `git commit`, `git push`,
  merge, 태그, GitHub Release, PyPI 배포를 실행하지 않는다. 필요하면 목적별 명령어를
  사용자에게 전달한다.
- 코드 중복과 버그·오류 가능성을 정리한다. 합의한 설계와 다른 구현을 발견하면 즉시
  사용자에게 알리고 수정 방향을 확인한다.
